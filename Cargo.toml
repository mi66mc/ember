[package]
name = "ember"
version = "0.1.0"
edition = "2024"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"

[dependencies]

[dev-dependencies]
criterion = "0.7"

[[bench]]
name = "vm"
harness = false
